10 REM TEST
20 PRINT "A"
30 INPUT A
40 GOTO 40
50 END
60 *SUB
70 C=A*B
80 IF A>B THEN 80
90 IF NOT A AND B THEN 90
91 REM controls
92 GOTO GOSUB RETURN IF THEN ELSE WHILE WEND FOR TO STEP NEXT ERROR RESUME CALL EXEC
95 REM statements
100 END PRINT INPUT DATA DIM READ LET RESTORE CLEAR WAIT DEF POKE OUT LPRINT WIDTH SWAP ERASE DEFSTR DEFINT DEFSNG DEFDBL LINE WRITE PLAY SOUND COMMON CHAIN OPTION BASE
105 RANDOMIZE OPEN FIELD GET PUT CLOSE LSET RSET COLOR CIRCLE CLS PSET PRESET PAINT SCREEN LOCATE BEEP PEN ON STOP VIEW WINDOW POINT COM
107 REM functions
110 TAB SPC ERL ERR STRING$ USING INSTR VARPTR INKEY$ LEFT$ RIGHT$ MID$ SGN INT ABS SQR RND SIN LOG EXP COS TAN ATN FRE INP POS LEN STR$ VAL ASC
115 CHR$ PEEK SPACE$ OCT$ HEX$ LPOS CINT CSNG CDBL FIX CVI CVS CVD EOF LOC LOF  MKI$ MKS$ MKD$ CSRLIN DATE$  TIME$ STICK STRIG
117 REM commands
120 RUN NEW TRON TROFF FILES NAME KILL BLOAD BSAVE KEY
125 REM operators
130 AND OR MOD  NOT
140 REM unrecognized statements/commands
150 CONSOLE BGM DSKO$ ROLL HELP PALET TALK KANJI CMD SET MERGE STATUS
151 LIST CONT LLIST EDIT MENU DELETE AUTO RENUM CLOAD CSAVE LOAD SAVE LFILES MON COPY TERM MOTOR 
152 REM unrecognized functions/operators
155 IMP PAD GRP$ SEARCH MAP SEARCH MAP OFF DSKF FPOS USR FN ATTR$ XOR EQV
